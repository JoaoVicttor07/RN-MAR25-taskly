import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
        gap: 5
    },
    input: {
        borderColor: "#5B3CC4",
        borderWidth: 3,
        borderRadius: 7,
        height: 50,
        paddingLeft: 15,
        color: "#1E1E1E"
    },
    erro: {
        color: "#E63946"
    }
})

export default styles